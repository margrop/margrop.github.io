<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://blog.margrop.net</id>
    <title>魔都水滴</title>
    <updated>2021-02-06T01:18:47.196Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://blog.margrop.net"/>
    <link rel="self" href="https://blog.margrop.net/atom.xml"/>
    <subtitle>A Programmer, A Javaer, A Father, A Geeker, A Huster, A Acmer, A Wower, A Player</subtitle>
    <logo>https://blog.margrop.net/images/avatar.png</logo>
    <icon>https://blog.margrop.net/favicon.ico</icon>
    <rights>All rights reserved 2021, 魔都水滴</rights>
    <entry>
        <title type="html"><![CDATA[Swagger-UI 增加项目依赖，修改index.html]]></title>
        <id>https://blog.margrop.net/post/swagger-ui-add-md5/</id>
        <link href="https://blog.margrop.net/post/swagger-ui-add-md5/">
        </link>
        <updated>2021-02-06T01:13:39.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="swagger-bundle-打包">swagger-bundle 打包</h1>
<p>首先进入<code>swagger-ui</code>目录</p>
<pre><code class="language-bash">npm install
npm run build
</code></pre>
<h1 id="如何修改">如何修改</h1>
<p>index.html中的url修改为：</p>
<pre><code class="language-js">url: &quot;/v2/api-docs?group=&quot; + getUrlParam(&quot;group&quot;),
</code></pre>
]]></summary>
        <content type="html"><![CDATA[<h1 id="swagger-bundle-打包">swagger-bundle 打包</h1>
<p>首先进入<code>swagger-ui</code>目录</p>
<pre><code class="language-bash">npm install
npm run build
</code></pre>
<h1 id="如何修改">如何修改</h1>
<p>index.html中的url修改为：</p>
<pre><code class="language-js">url: &quot;/v2/api-docs?group=&quot; + getUrlParam(&quot;group&quot;),
</code></pre>
<!-- more -->
<p>并且增加函数</p>
<pre><code class="language-js">    function getUrlParam(name) {
        var reg = new RegExp(&quot;(^|&amp;)&quot; + name + &quot;=([^&amp;]*)(&amp;|$)&quot;); //构造一个含有目标参数的正则表达式对象
        var r = window.location.search.substr(1).match(reg);  //匹配目标参数
        if (r != null) return unescape(r[2]);
        return &quot;&quot;; //返回参数值
    }
</code></pre>
<p><img src="https://blog.margrop.net/post-images/swagger-ui-1.jpg" alt="" loading="lazy"><br>
<img src="https://blog.margrop.net/post-images/swagger-ui-2.jpg" alt="" loading="lazy"></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Swagger-JS 如何增加MD5校验，和项目依赖]]></title>
        <id>https://blog.margrop.net/post/swagger-js-add-md5/</id>
        <link href="https://blog.margrop.net/post/swagger-js-add-md5/">
        </link>
        <updated>2021-02-06T01:05:25.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="swagger-bundle-打包">swagger-bundle 打包</h1>
<p>首先进入<code>swagger-js</code>目录</p>
<pre><code class="language-bash">npm install
npm run build
</code></pre>
<h1 id="如何修改">如何修改</h1>
<p>在<code>src/execute.js</code>文件中增加代码：</p>
<pre><code class="language-js">import md5 from 'md5';
</code></pre>
]]></summary>
        <content type="html"><![CDATA[<h1 id="swagger-bundle-打包">swagger-bundle 打包</h1>
<p>首先进入<code>swagger-js</code>目录</p>
<pre><code class="language-bash">npm install
npm run build
</code></pre>
<h1 id="如何修改">如何修改</h1>
<p>在<code>src/execute.js</code>文件中增加代码：</p>
<pre><code class="language-js">import md5 from 'md5';
</code></pre>
<!-- more -->
<p>在<code>export function buildRequest({</code>方法类<code>mergeInQueryOrForm(req)</code>语句前增加代码：</p>
<pre><code class="language-js">  //calcSn for server
  var req = request
  console.log(req.body);
  if (req.body) {
    let bodyObj = JSON.parse(req.body);
    bodyObj['header']['token'] = req['headers']['token'];
    bodyObj['header']['snTime'] = (new Date()).valueOf();

    let allData = Object.assign(JSON.parse(JSON.stringify(bodyObj['body'])), {
      token: bodyObj['header']['token'],
      snTime: bodyObj['header']['snTime']
    });
    bodyObj['header']['sn'] = calcSn(allData);
    req.body = JSON.stringify(bodyObj);
    req.headers['X-Sn-Verify'] = md5(req.body);
  }
  console.log(req.body);

  function calcSn(allData) {
    let queryString = &quot;&quot;;
    Object.keys(allData)
      .sort()
      .forEach(function (key, i) {
        let value = allData[key];
        queryString += key + &quot;=&quot; + value + &quot;&amp;&quot;;
      });
    if (queryString.length &gt; 0) {
      queryString = queryString.substring(0, queryString.length - 1)
    }
    return md5(queryString);
  }
</code></pre>
<p><code>package.json</code>需要增加跨平台编译的支持<br>
<img src="https://blog.margrop.net/post-images/swagger-js-1.jpg" alt="" loading="lazy"><br>
<img src="https://blog.margrop.net/post-images/swagger-js-2.jpg" alt="" loading="lazy"></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Proxmox VE 环境下，如何安装集客AC虚拟机？]]></title>
        <id>https://blog.margrop.net/post/how-to-install-jika-ac-in-pve/</id>
        <link href="https://blog.margrop.net/post/how-to-install-jika-ac-in-pve/">
        </link>
        <updated>2021-02-03T08:18:33.000Z</updated>
        <summary type="html"><![CDATA[<p>根据<a href="https://www.right.com.cn/forum/thread-1501038-1-1.html">恩山的集客AC AP扫盲贴</a>介绍：</p>
<blockquote>
<p>问：可以不用<code>AC</code>，直接两个<code>AP</code>连接主路由漫游么？<br>
答：可以的，但有<code>AC</code>的漫游效果比没有好；有<code>ac</code>在，漫游效果不会差的，集客的漫游不是简单的弱踢。有<code>ac</code>在，不管集客<code>ap</code>支持不支持<code>kvr</code>，都有很好的效果，因为<code>ac+ap</code>是主动漫游，主动漫游走的不是<code>kvr</code>协议，对客户端要求也很低。微<code>ac</code>只负责下发配置，不能主动漫游，漫游效果依赖客户端。</p>
</blockquote>
]]></summary>
        <content type="html"><![CDATA[<p>根据<a href="https://www.right.com.cn/forum/thread-1501038-1-1.html">恩山的集客AC AP扫盲贴</a>介绍：</p>
<blockquote>
<p>问：可以不用<code>AC</code>，直接两个<code>AP</code>连接主路由漫游么？<br>
答：可以的，但有<code>AC</code>的漫游效果比没有好；有<code>ac</code>在，漫游效果不会差的，集客的漫游不是简单的弱踢。有<code>ac</code>在，不管集客<code>ap</code>支持不支持<code>kvr</code>，都有很好的效果，因为<code>ac+ap</code>是主动漫游，主动漫游走的不是<code>kvr</code>协议，对客户端要求也很低。微<code>ac</code>只负责下发配置，不能主动漫游，漫游效果依赖客户端。</p>
</blockquote>
<!-- more -->
<h1 id="集客ac简介">集客AC简介</h1>
<p>之前家里的好几台集客<code>AP</code>一直没有使用<code>AC</code>集中管理。<br>
使用家庭环境的PVE后，可以无成本再添加一台集客<code>AC</code>的虚拟机，集中管理家里的各<code>AP</code>和各种无线设备，岂不妙哉。</p>
<p>下面是安装完成后的<code>AC</code>首页：<br>
<img src="https://blog.margrop.net/post-images/jike-ac-index-1.jpg" alt="" loading="lazy"></p>
<h1 id="安装模式">安装模式</h1>
<p>建议使用<code>旁AC</code>的模式安装，这样<code>AC</code>只负责管理<code>AP</code>，至于什么外网接入，<code>DHCP</code>什么的，一律不负责。</p>
<h1 id="下载ac-x86固件">下载AC x86固件</h1>
<ul>
<li>下载<a href="https://www.lanzous.com/i9ccb1a">vhd格式的安装包</a></li>
<li>访问<a href="https://gecoos.lanzous.com/b01bcswyj">集客的官方蓝奏云地址</a>，下载最新版的升级包</li>
</ul>
<h1 id="导入虚拟机镜像">导入虚拟机镜像</h1>
<ul>
<li>可参考之前的文章<a href="https://blog.margrop.net/post/pve-kvm-import-vm-format-qcow2/">在 Proxmox VE 里面，如何导入第三方的虚拟机镜像？</a></li>
</ul>
<h1 id="开机并安装集客ac">开机并安装集客AC</h1>
<ul>
<li>在<code>PVE</code>中打开虚拟机，连接虚拟机</li>
<li>等待成功启动后，安装的版本选择<code>3&gt; AC控制器版</code></li>
<li>安装完成后，在<code>PVE</code>的管理界面，关闭虚拟机，删除安装磁盘，点击“保存”，否则会重复安装过程。</li>
</ul>
<h1 id="参考文章">参考文章</h1>
<p><a href="https://www.right.com.cn/forum/thread-2101274-1-1.html">黑群晖安装集客AC控制器简明教程（更新vhd格式蓝奏云下载）</a><br>
<a href="https://post.smzdm.com/p/ag87mg6m/">爱快软路由虚拟机安装集客网关AC控制器</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Let's Encrypt自动续期后自动同步Proxmox VE和DSM证书]]></title>
        <id>https://blog.margrop.net/post/lets-encrypt-sync-https-cert-to-pve-and-dsm/</id>
        <link href="https://blog.margrop.net/post/lets-encrypt-sync-https-cert-to-pve-and-dsm/">
        </link>
        <updated>2021-02-02T11:19:38.000Z</updated>
        <summary type="html"><![CDATA[<p>上次我们讲了<a href="https://blog.margrop.net/post/lets-encrypt-use-dns-txt-acme-challenge-domain-available/">Let's Encrypt通过DNS TXT记录来验证域名有效性</a>，其中文章最后写了，如何使用<code>certbot renew</code>进行自动续期。那自动续期成功了，如何同步到<code>PVE</code>和<code>DSM</code>呢？</p>
]]></summary>
        <content type="html"><![CDATA[<p>上次我们讲了<a href="https://blog.margrop.net/post/lets-encrypt-use-dns-txt-acme-challenge-domain-available/">Let's Encrypt通过DNS TXT记录来验证域名有效性</a>，其中文章最后写了，如何使用<code>certbot renew</code>进行自动续期。那自动续期成功了，如何同步到<code>PVE</code>和<code>DSM</code>呢？</p>
<!-- more -->
<p>下面我们使用脚本来进行自动同步。</p>
<ul>
<li>需要在<code>Proxmox VE</code>和<code>DSM</code>服务器配置<code>SSH</code>免密登录</li>
</ul>
<h1 id="自动同步proxmox-ve证书">自动同步Proxmox VE证书</h1>
<ul>
<li>同步到本机的<code>Proxmox VE</code></li>
</ul>
<pre><code class="language-bash">cp /etc/letsencrypt/live/blog.margrop.net/fullchain.pem /etc/pve/local/pveproxy-ssl.pem
cp /etc/letsencrypt/live/blog.margrop.net/privkey.pem /etc/pve/local/pveproxy-ssl.key
systemctl restart pveproxy
</code></pre>
<ul>
<li>同步到局域网的<code>Proxmox VE</code></li>
</ul>
<pre><code class="language-bash">scp  /etc/letsencrypt/live/blog.margrop.net/fullchain.pem root@192.168.1.155:/etc/pve/local/pveproxy-ssl.pem
scp  /etc/letsencrypt/live/blog.margrop.net/privkey.pem root@192.168.1.155:/etc/pve/local/pveproxy-ssl.key
ssh root@192.168.1.155 &quot;systemctl restart pveproxy&quot;
</code></pre>
<h1 id="自动同步dsm证书">自动同步DSM证书</h1>
<ul>
<li>同步到局域网的<code>DSM</code></li>
<li>脚本里面的<code>gnEsPP</code>，每个群晖的路径都不一样，请根据实际情况自行替换。</li>
</ul>
<pre><code class="language-bash">scp  /etc/letsencrypt/live/blog.margrop.net/fullchain.pem root@192.168.1.55:/usr/syno/etc/certificate/_archive/gnEsPP/fullchain.pem
scp  /etc/letsencrypt/live/blog.margrop.net/privkey.pem   root@192.168.1.55:/usr/syno/etc/certificate/_archive/gnEsPP/privkey.pem
scp  /etc/letsencrypt/live/blog.margrop.net/chain.pem     root@192.168.1.55:/usr/syno/etc/certificate/_archive/gnEsPP/chain.pem
scp  /etc/letsencrypt/live/blog.margrop.net/cert.pem      root@192.168.1.55:/usr/syno/etc/certificate/_archive/gnEsPP/cert.pem
scp  /etc/letsencrypt/live/blog.margrop.net/fullchain.pem root@192.168.1.55:/usr/syno/etc/certificate/system/default/fullchain.pem
scp  /etc/letsencrypt/live/blog.margrop.net/privkey.pem   root@192.168.1.55:/usr/syno/etc/certificate/system/default/privkey.pem
scp  /etc/letsencrypt/live/blog.margrop.net/chain.pem     root@192.168.1.55:/usr/syno/etc/certificate/system/default/chain.pem
scp  /etc/letsencrypt/live/blog.margrop.net/cert.pem      root@192.168.1.55:/usr/syno/etc/certificate/system/default/cert.pem
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[在 Proxmox VE 里面，如何导入第三方的虚拟机镜像？]]></title>
        <id>https://blog.margrop.net/post/pve-kvm-import-vm-format-qcow2/</id>
        <link href="https://blog.margrop.net/post/pve-kvm-import-vm-format-qcow2/">
        </link>
        <updated>2021-02-01T07:12:47.000Z</updated>
        <summary type="html"><![CDATA[<p>下面的例子，<code>虚拟机ID</code>均以<code>100</code>为例，需自行修改。</p>
<h1 id="文件格式转换">文件格式转换</h1>
<ul>
<li>使用qemu-img 文件将其他格式的虚拟机硬盘文件转出<code>qcow2</code>格式</li>
<li>例如，下面是将<code>vmdk</code>格式转换为<code>qcow2</code>格式</li>
</ul>
]]></summary>
        <content type="html"><![CDATA[<p>下面的例子，<code>虚拟机ID</code>均以<code>100</code>为例，需自行修改。</p>
<h1 id="文件格式转换">文件格式转换</h1>
<ul>
<li>使用qemu-img 文件将其他格式的虚拟机硬盘文件转出<code>qcow2</code>格式</li>
<li>例如，下面是将<code>vmdk</code>格式转换为<code>qcow2</code>格式</li>
</ul>
<!-- more -->
<pre><code class="language-bash">qemu-img convert -f vmdk /mnt/usb/windows-server/windows-server.vmdk -O qcow2 /var/lib/vz/images/100/windows-server.qcow2
</code></pre>
<h1 id="增加虚拟磁盘">增加虚拟磁盘</h1>
<ul>
<li>先在Web界面增加一个<code>local</code>的虚拟磁盘，比如自动生成了<code>vm-100-disk-0.qcow2</code></li>
</ul>
<h1 id="qcow2文件类型">qcow2文件类型</h1>
<p>将该镜像复制到 <code>/var/lib/vz/images/100/</code> 这个目录下面</p>
<h1 id="编辑虚拟机配置文件">编辑虚拟机配置文件</h1>
<pre><code class="language-bash">vim /etc/pve/qemu-server/100.conf
</code></pre>
<p>找到<code>vm-100-disk-0.qcow2</code>，将这个文件改成需要的文件名即可。</p>
<h1 id="删除空的虚拟磁盘">删除空的虚拟磁盘</h1>
<ul>
<li>手工进入<code>/var/lib/vz/images/100/</code>目录，删除第一步新建的空虚拟磁盘文件<code>vm-100-disk-0.qcow2</code></li>
</ul>
<pre><code class="language-bash">cd /var/lib/vz/images/100/
rm vm-100-disk-0.qcow2
</code></pre>
<p>参考文章<br>
<a href="https://manpages.debian.org/testing/qemu-utils/qemu-img.1.en.html">QEMU-IMG</a><br>
<a href="https://pve.proxmox.com/wiki/Migration_of_servers_to_Proxmox_VE#Move_the_image_to_the_Proxmox_VE_Server">Migration of servers to Proxmox VE</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[如何通过 NPM 安装 Node-RED]]></title>
        <id>https://blog.margrop.net/post/how-to-install-nodered-with-npm/</id>
        <link href="https://blog.margrop.net/post/how-to-install-nodered-with-npm/">
        </link>
        <updated>2021-02-01T01:57:07.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="node-red是什么"><code>Node-RED</code>是什么</h1>
<p><code>Node-RED</code>是一种低代码/无代码编程工具，用于以新颖有趣的方式将<code>硬件设备</code>，<code>API</code>和<code>在线服务</code>连接在一起。特别适用于<code>智能家居</code>。</p>
<p><code>Node-RED</code>提供了一个基于浏览器的流编辑器，可轻松使用面板中的各种节点将流连接在一起。然后，单击即可将流部署到运行时。</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="node-red是什么"><code>Node-RED</code>是什么</h1>
<p><code>Node-RED</code>是一种低代码/无代码编程工具，用于以新颖有趣的方式将<code>硬件设备</code>，<code>API</code>和<code>在线服务</code>连接在一起。特别适用于<code>智能家居</code>。</p>
<p><code>Node-RED</code>提供了一个基于浏览器的流编辑器，可轻松使用面板中的各种节点将流连接在一起。然后，单击即可将流部署到运行时。</p>
<!-- more -->
<blockquote>
<p>可以使用富文本编辑器在编辑器中创建<code>JavaScript</code>函数。<br>
内置库允许您保存有用的功能，模板或流程以供重复使用。</p>
</blockquote>
<h2 id="建立在nodejs之上">建立在Node.js之上</h2>
<blockquote>
<p>基于<code>Node.js</code>构建，轻量级的运行环境，充分利用了事件驱动的非阻塞模型。这使得它非常适合在低成本的硬件（如<code>Raspberry Pi</code>）上的网络边缘以及云服务器运行。</p>
</blockquote>
<blockquote>
<p>Node的软件包存储库中有超过225,000个模块，可以轻松扩展面板节点的范围以添加新功能。</p>
</blockquote>
<h2 id="社交共享">社交共享</h2>
<blockquote>
<p>在<code>Node-RED</code>中创建的流使用<code>JSON</code>存储，可以轻松导入和导出以与他人共享。</p>
</blockquote>
<blockquote>
<p>在线流程库使您可以与世界分享最佳流程。</p>
</blockquote>
<h1 id="安装node和npm环境">安装Node和NPM环境</h1>
<ul>
<li>之前有写过<a href="https://blog.margrop.net/post/centos-7-install-node-and-npm-latest/"><code>CentOS 7 安装最新版本Node和NPM</code></a>，CentOS 7可以参考</li>
<li>其他Linux建议参考<a href="https://github.com/nodejs/help/wiki/Installation"><code>在 Linux 上，通过二进制文件安装 Node.js</code></a></li>
<li>其他OS建议参考<a href="https://github.com/nodejs/node/blob/master/BUILDING.md#building-nodejs-on-supported-platforms"><code>在支持的平台上，使用源代码构建 Node.js</code></a></li>
</ul>
<h1 id="使用npm安装">使用NPM安装</h1>
<p>要安装<code>Node-RED</code>，您可以使用<code>node.js</code>附带的<code>npm</code>命令：</p>
<pre><code class="language-bash">sudo npm install -g --unsafe-perm node-red
</code></pre>
<p>该命令会将<code>Node-RED</code>及其依赖项安装为全局模块。<br>
如果命令输出的结尾类似于以下内容，则可以确认它已成功：</p>
<pre><code class="language-bash">+ node-red@1.1.0
added 332 packages from 341 contributors in 18.494s
found 0 vulnerabilities
</code></pre>
<h1 id="运行node-red">运行Node-Red</h1>
<p>安装为全局模块后，您可以使用node-red命令在终端中启动Node-RED。您可以使用Ctrl-C或关闭终端窗口来停止Node-RED。</p>
<pre><code class="language-bash">$ node-red

Welcome to Node-RED
===================

30 Jun 23:43:39 - [info] Node-RED version: v1.1.0
30 Jun 23:43:39 - [info] Node.js  version: v10.21.0
30 Jun 23:43:39 - [info] Darwin 18.7.0 x64 LE
30 Jun 23:43:39 - [info] Loading palette nodes
30 Jun 23:43:44 - [warn] rpi-gpio : Raspberry Pi specific node set inactive
30 Jun 23:43:44 - [info] Settings file  : /Users/nol/.node-red/settings.js
30 Jun 23:43:44 - [info] HTTP Static    : /Users/nol/node-red/web
30 Jun 23:43:44 - [info] Context store  : 'default' [module=localfilesystem]
30 Jun 23:43:44 - [info] User directory : /Users/nol/.node-red
30 Jun 23:43:44 - [warn] Projects disabled : set editorTheme.projects.enabled=true to enable
30 Jun 23:43:44 - [info] Creating new flows file : flows_noltop.json
30 Jun 23:43:44 - [info] Starting flows
30 Jun 23:43:44 - [info] Started flows
30 Jun 23:43:44 - [info] Server now running at http://127.0.0.1:1880/red/
</code></pre>
<p>然后，您可以通过将浏览器指向<code>http://localhost:1880</code>来访问<code>Node-RED</code>编辑器。</p>
<p>日志输出为您提供各种信息：</p>
<ul>
<li><code>Node-RED</code>和<code>Node.js</code>的版本</li>
<li>尝试加载调色板节点时遇到任何错误</li>
<li>设置文件和用户目录的位置</li>
<li>它正在使用的流文件的名称。<br>
<code>Node-RED</code>使用<code>flows_&lt;hostname&gt;.json</code>作为默认流文件。您可以通过提供流文件名作为<code>node-red</code>命令的参数来更改此设置。</li>
</ul>
<h1 id="使用命令行">使用命令行</h1>
<p>可以使用命令启动Node-RED node-red。该命令可以使用各种参数：</p>
<pre><code class="language-bash">
node-red [-v] [-?] [--settings settings.js] [--userDir DIR]
         [--port PORT] [--title TITLE] [--safe] [flows.json|projectName]
         [-D X=Y|@file]
</code></pre>
<table>
<thead>
<tr>
<th>Option</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>-p, --port</td>
<td>设置运行时侦听的TCP端口。默认：1880</td>
</tr>
<tr>
<td>--safe</td>
<td>在不启动流程的情况下启动Node-RED。<br>这使您可以在编辑器中打开流并进行更改，而无需运行流。<br>当您部署更改时，流程就开始了。</td>
</tr>
<tr>
<td>-s, --settings FILE</td>
<td>设置要使用的设置文件。默认值：<code>&lt;userDir&gt;/settings.js</code></td>
</tr>
<tr>
<td>--title TITLE</td>
<td>设置进程窗口标题</td>
</tr>
<tr>
<td>-u, --userDir DIR</td>
<td>设置要使用的用户目录。默认：<code>~/.node-red</code></td>
</tr>
<tr>
<td>-v</td>
<td>启用详细输出</td>
</tr>
<tr>
<td>-?, --help</td>
<td>显示命令行用法帮助并退出</td>
</tr>
<tr>
<td>flows.json</td>
<td>projectName</td>
</tr>
</tbody>
</table>
<p><code>Node-RED</code>使用<code>flows_&lt;hostname&gt;.json</code>作为默认流文件。如果运行的计算机可能会更改其主机名，则应确保提供一个静态文件名；作为命令行参数或使用设置文件中的flowsFile选项。</p>
<h1 id="如何升级node-red">如何升级Node-RED</h1>
<p>如果已将Node-RED作为全局npm软件包安装，则可以使用以下命令升级到最新版本：</p>
<pre><code class="language-bash">sudo npm install -g --unsafe-perm node-red
</code></pre>
<h1 id="参考文章">参考文章</h1>
<p><a href="https://nodered.org/">Node-Red</a><br>
<a href="https://nodejs.org/zh-cn/download/">Node-JS</a><br>
<a href="https://nodered.org/docs/getting-started/local#installing-with-npm">Running Node-RED locally</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[如何搭建开源的Web流程图工具 Diagrams.net（原draw.io）]]></title>
        <id>https://blog.margrop.net/post/how-to-install-docker-drawio/</id>
        <link href="https://blog.margrop.net/post/how-to-install-docker-drawio/">
        </link>
        <updated>2021-01-30T09:59:08.000Z</updated>
        <summary type="html"><![CDATA[<p><code>ProcessOn</code>目前已经是大家熟知的Web的<code>流程图</code>，<code>UML</code>等绘图工具了。但 <code>ProcessOn</code> 是收费服务，免费的又限制太多，那还有免费的午餐吗？</p>
<p>有，就是<code>Diagrams.net</code>。<strong>完全免费，功能强大~</strong></p>
]]></summary>
        <content type="html"><![CDATA[<p><code>ProcessOn</code>目前已经是大家熟知的Web的<code>流程图</code>，<code>UML</code>等绘图工具了。但 <code>ProcessOn</code> 是收费服务，免费的又限制太多，那还有免费的午餐吗？</p>
<p>有，就是<code>Diagrams.net</code>。<strong>完全免费，功能强大~</strong></p>
<!-- more -->
<h1 id="在线使用diagramsnet">在线使用<code>Diagrams.net</code></h1>
<p><code>Diagrams.net</code>的使用方法非常简单，直接打开浏览器，输入网址 <a href="https:/draw.io">draw.io</a>，或 <a href="https://app.diagrams.net/">app.diagrams.net</a>，可以使用第三方存储，支持<code>Google Drive</code>，<code>OneDrive</code>，<code>Dropbox</code>，<code>Github</code>，<code>GitLab</code>，<code>本地文件</code>，<code>浏览器缓存</code>，保存的文件格式也有好多种，具体有『XML File(<code>.drawio</code>)』，『Editable Bitmap Image(<code>.png</code>)』，『Editable Vector Image(<code>.svg</code>)』，『HTML File(<code>.html)</code>』，『XML File(<code>.xml</code>)』，</p>
<p>如果不需要自己搭建 Diagrams.net 服务，那么这篇文章到这里已经结束了。</p>
<h1 id="安装docker环境">安装Docker环境</h1>
<ul>
<li>之前有写过<a href="https://blog.margrop.net/post/centos-7-install-docker-ce-and-update-source/">如何安装Docker</a>，CentOS 7可以参考，其他OS建议参考<a href="https://docs.docker.com/engine/install/">官方文档</a></li>
</ul>
<h1 id="安装docker-drawio">安装Docker-DrawIO</h1>
<ul>
<li>Docker命令</li>
</ul>
<pre><code class="language-bash">docker run -it --name=&quot;draw&quot; -p 8080:8080 -p 8443:8443 jgraph/drawio
docker start draw
</code></pre>
<ul>
<li>Docker开机自动启动镜像</li>
</ul>
<pre><code class="language-bash">docker update --restart=always draw
</code></pre>
<h1 id="打开防火墙8080端口">打开防火墙8080端口</h1>
<pre><code class="language-bash">firewall-cmd --zone=public --add-port=8080/tcp --permanent
firewall-cmd --reload
</code></pre>
<h1 id="参考文章">参考文章</h1>
<p><a href="https://docs.docker.com/engine/install/">Install Docker Engine</a><br>
<a href="https://github.com/jgraph/docker-drawio">Github : jgraph/docker-drawio</a><br>
<a href="https://blog.csdn.net/wxb880114/article/details/82904765">reboot 后 Docker服务及容器自动启动设置</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[PVE下如何配置新安装的 ubuntu 20.04 server 虚拟机]]></title>
        <id>https://blog.margrop.net/post/pve-how-to-config-brand-new-ubuntu-20.04-vm/</id>
        <link href="https://blog.margrop.net/post/pve-how-to-config-brand-new-ubuntu-20.04-vm/">
        </link>
        <updated>2021-01-30T07:33:44.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="下载安装镜像iso">下载安装镜像ISO</h1>
<p><a href="https://mirrors.aliyun.com/ubuntu-releases/focal/ubuntu-20.04.1-live-server-amd64.iso">阿里云ubuntu-20.04-live-server下载</a></p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="下载安装镜像iso">下载安装镜像ISO</h1>
<p><a href="https://mirrors.aliyun.com/ubuntu-releases/focal/ubuntu-20.04.1-live-server-amd64.iso">阿里云ubuntu-20.04-live-server下载</a></p>
<!-- more -->
<h1 id="新建虚拟机">新建虚拟机</h1>
<ul>
<li>略</li>
</ul>
<h1 id="插入iso镜像安装系统">插入ISO镜像，安装系统</h1>
<ul>
<li>略</li>
</ul>
<h1 id="修改apt-get为aliyun源">修改apt-get为aliyun源：</h1>
<ul>
<li>用你熟悉的编辑器打开<code>/etc/apt/sources.list</code>，编辑为下面的内容</li>
</ul>
<pre><code class="language-bash">deb http://mirrors.aliyun.com/ubuntu/ focal main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ focal main restricted universe multiverse

deb http://mirrors.aliyun.com/ubuntu/ focal-security main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ focal-security main restricted universe multiverse

deb http://mirrors.aliyun.com/ubuntu/ focal-updates main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ focal-updates main restricted universe multiverse

deb http://mirrors.aliyun.com/ubuntu/ focal-proposed main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ focal-proposed main restricted universe multiverse

deb http://mirrors.aliyun.com/ubuntu/ focal-backports main restricted universe multiverse
deb-src http://mirrors.aliyun.com/ubuntu/ focal-backports main restricted universe multiverse
</code></pre>
<h1 id="安装基本网络工具">安装基本网络工具</h1>
<pre><code class="language-bash">sudo apt install net-tools
</code></pre>
<h1 id="修改root密码为vm">修改root密码为vm</h1>
<p>输入 sudo passwd 命令，然后会提示输入当前用户的密码。<br>
按enter键，终端会提示输入新的密码并确认，此时的密码就是新的root密码。<br>
修改完毕以后，在执行su root命令，此时输入新的root密码即可。</p>
<h1 id="新增ssh免密登录">新增SSH免密登录</h1>
<pre><code class="language-bash">cd ~
mkdir .ssh
echo ssh-rsa  Administrator@PCOS-1407251925 &gt;&gt; ~/.ssh/authorized_keys
</code></pre>
<h1 id="ssh启用允许root远程连接">SSH启用允许root远程连接：</h1>
<pre><code class="language-bash">sudo vim /etc/ssh/sshd_config
</code></pre>
<ul>
<li>将<code>PermitRootLogin</code>这一项取消注释，并改为<code>yes</code></li>
</ul>
<pre><code class="language-bash">#LoginGraceTime 2m
#PermitRootLogin prohibit-password
PermitRootLogin yes
#StrictModes yes
#MaxAuthTries 6
#MaxSessions 10
</code></pre>
<h1 id="安装qemu-guest-agent">安装qemu-guest-agent</h1>
<ul>
<li>参考<a href="https://pve.proxmox.com/wiki/Qemu-guest-agent">https://pve.proxmox.com/wiki/Qemu-guest-agent</a></li>
</ul>
<pre><code class="language-bash">apt-get install qemu-guest-agent
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[如何使用通用的Hibernate类型映射JSON对象]]></title>
        <id>https://blog.margrop.net/post/how-to-map-json-collections-using-jpa-and-hibernate/</id>
        <link href="https://blog.margrop.net/post/how-to-map-json-collections-using-jpa-and-hibernate/">
        </link>
        <updated>2021-01-29T08:06:09.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="介绍">介绍</h1>
<p>在本文中，我们将看到如何使用<a href="http://www.google.com/" title="Hibernate Types">Hibernate Types</a>开源项目将JSON列映射到JPA实体属性。</p>
<p>虽然您可以创建自己的自定义Hibernate类型，但可以在Oracle，SQL Server，PostgreSQL或MySQL上映射JSON列类型，但是由于<a href="http://www.google.com/" title="Hibernate Types">Hibernate Types</a>项目已经提供了此功能，因此您无需实现自己的Hibernate Type 。</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="介绍">介绍</h1>
<p>在本文中，我们将看到如何使用<a href="http://www.google.com/" title="Hibernate Types">Hibernate Types</a>开源项目将JSON列映射到JPA实体属性。</p>
<p>虽然您可以创建自己的自定义Hibernate类型，但可以在Oracle，SQL Server，PostgreSQL或MySQL上映射JSON列类型，但是由于<a href="http://www.google.com/" title="Hibernate Types">Hibernate Types</a>项目已经提供了此功能，因此您无需实现自己的Hibernate Type 。</p>
<!-- more -->
<h1 id="领域模型">领域模型</h1>
<p>假设我们具有以下域模型：<br>
<img src="https://vladmihalcea.com/wp-content/uploads/2016/06/jsontypedomainmodel.png" alt="JsonType域模型" loading="lazy"></p>
<p>Location和Ticket是JSON Object(s)，而Event和Participant是JPA实体。我们的目标是提供一种Type适用于任何类型的JSON JavaObject以及支持JSON列的任何关系数据库的Hibernate JSON 。</p>
<h1 id="maven依赖">Maven依赖</h1>
<p>您需要做的第一件事是在项目pom.xml配置文件中设置以下Maven依赖项：</p>
<pre><code class="language-xml">&lt;dependency&gt;
    &lt;groupId&gt;com.vladmihalcea&lt;/groupId&gt;
    &lt;artifactId&gt;hibernate-types-52&lt;/artifactId&gt;
    &lt;version&gt;${hibernate-types.version}&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>如果您使用的是Hibernate的旧版本，请查看<a href="https://github.com/vladmihalcea/hibernate-types">hibernate-types<code>GitHub存储库</code></a>，以获取有关当前Hibernate版本的匹配依赖项的更多信息。</p>
<h1 id="声明hibernate-types">声明Hibernate Types</h1>
<p>要使用JSON Hibernate Types，我们必须使用@TypeDef注释声明它们：</p>
<pre><code class="language-java">@TypeDefs({
    @TypeDef(name = &quot;json&quot;, typeClass = JsonStringType.class),
    @TypeDef(name = &quot;jsonb&quot;, typeClass = JsonBinaryType.class)
})
@MappedSuperclass
public class BaseEntity {
    //Code omitted for brevity
}
</code></pre>
<blockquote>
<p>TypeDef批注可以应用于基本实体类，也可以应用于与当前实体包关联的package-info.java文件。</p>
</blockquote>
<h1 id="mysql">MySQL</h1>
<p>MySQL 5.7增加了对JSON类型的支持，在JDBC级别上，需要将其实现为String。因此，我们将使用JsonStringType。</p>
<p>实体映射如下所示：</p>
<pre><code class="language-java">@Entity(name = &quot;Event&quot;)
@Table(name = &quot;event&quot;)
public class Event extends BaseEntity {
 
    @Type(type = &quot;json&quot;)
    @Column(columnDefinition = &quot;json&quot;)
    private Location location;
 
    public Location getLocation() {
        return location;
    }
 
    public void setLocation(Location location) {
        this.location = location;
    }
}
 
@Entity(name = &quot;Participant&quot;)
@Table(name = &quot;participant&quot;)
public class Participant extends BaseEntity {
 
    @Type(type = &quot;json&quot;)
    @Column(columnDefinition = &quot;json&quot;)
    private Ticket ticket;
 
    @ManyToOne
    private Event event;
 
    public Ticket getTicket() {
        return ticket;
    }
 
    public void setTicket(Ticket ticket) {
        this.ticket = ticket;
    }
 
    public Event getEvent() {
        return event;
    }
 
    public void setEvent(Event event) {
        this.event = event;
    }
}
</code></pre>
<p>插入以下实体时：</p>
<pre><code class="language-java">final AtomicReference&lt;Event&gt; eventHolder = new AtomicReference&lt;&gt;();
final AtomicReference&lt;Participant&gt; participantHolder = new AtomicReference&lt;&gt;();
 
doInJPA(entityManager -&gt; {
    Event nullEvent = new Event();
    nullEvent.setId(0L);
    entityManager.persist(nullEvent);
 
    Location location = new Location();
    location.setCountry(&quot;Romania&quot;);
    location.setCity(&quot;Cluj-Napoca&quot;);
 
    Event event = new Event();
    event.setId(1L);
    event.setLocation(location);
    entityManager.persist(event);
 
    Ticket ticket = new Ticket();
    ticket.setPrice(12.34d);
    ticket.setRegistrationCode(&quot;ABC123&quot;);
 
    Participant participant = new Participant();
    participant.setId(1L);
    participant.setTicket(ticket);
    participant.setEvent(event);
 
    entityManager.persist(participant);
 
    eventHolder.set(event);
    participantHolder.set(participant);
});
</code></pre>
<p>Hibernate生成以下语句：</p>
<pre><code class="language-java">INSERT INTO event (location, id)
VALUES (NULL(OTHER), 0)
 
INSERT INTO event (location, id)
VALUES ('{&quot;country&quot;:&quot;Romania&quot;,&quot;city&quot;:&quot;Cluj-Napoca&quot;}', 1)
 
INSERT INTO participant (event_id, ticket, id)
VALUES (1, {&quot;registrationCode&quot;:&quot;ABC123&quot;,&quot;price&quot;:12.34}, 1)
</code></pre>
<p>JSONObject(s)已正确实现在其关联的数据库列中。</p>
<p>不仅JSONObject(s)已从其数据库表示形式正确转换：</p>
<pre><code class="language-java">Event event = entityManager.find(Event.class, eventHolder.get().getId());
 
assertEquals(&quot;Cluj-Napoca&quot;, event.getLocation().getCity());
 
Participant participant = entityManager.find(
    Participant.class, participantHolder.get().getId());
assertEquals(&quot;ABC123&quot;, participant.getTicket().getRegistrationCode());
</code></pre>
<p>但是我们甚至可以发出基于JSON的本地SQL查询：</p>
<pre><code class="language-java">List&lt;String&gt; participants = entityManager.createNativeQuery(
    &quot;SELECT p.ticket -&gt; \&quot;$.registrationCode\&quot; &quot; +
    &quot;FROM participant p &quot; +
    &quot;WHERE JSON_EXTRACT(p.ticket, \&quot;$.price\&quot;) &gt; 1 &quot;)
.getResultList();
</code></pre>
<p>Object(s)可以修改JSON ：</p>
<pre><code class="language-java">event.getLocation().setCity(&quot;Constanța&quot;);
entityManager.flush();
</code></pre>
<p>Hibernate生成正确的UPDATE语句：</p>
<pre><code class="language-java">UPDATE event
SET location = '{&quot;country&quot;:&quot;Romania&quot;,&quot;city&quot;:&quot;Constanța&quot;}'
WHERE id = 1
</code></pre>
<h1 id="postgresql">PostgreSQL</h1>
<p>从9.2版开始，PostgreSQL就一直支持JSON类型。有两种类型可以使用：</p>
<ul>
<li>json</li>
<li>jsonb<br>
两种PostgreSQL JSON类型都需要使用二进制数据格式来实现，因此我们需要使用JsonBinaryType这次。</li>
</ul>
<h1 id="postgresql-json列类型">PostgreSQL JSON列类型</h1>
<p>对于JSON列类型，Object(s)必须如下更改两个JSON映射：</p>
<pre><code class="language-java">@Type(type = &quot;jsonb&quot;)
@Column(columnDefinition = &quot;json&quot;)
private Location location;
 
@Type(type = &quot;jsonb&quot;)
@Column(columnDefinition = &quot;json&quot;)
private Ticket ticket;
</code></pre>
<p>插入和实体更新都一样，甚至可以JSON按以下方式查询该列：</p>
<pre><code class="language-java">List&lt;String&gt; participants = entityManager.createNativeQuery(
    &quot;SELECT p.ticket -&gt;&gt;'registrationCode' &quot; +
    &quot;FROM participant p &quot; +
    &quot;WHERE p.ticket -&gt;&gt; 'price' &gt; '10'&quot;)
.getResultList();
</code></pre>
<h1 id="postgresql-jsonb列类型">PostgreSQL JSONB列类型</h1>
<p>对于JSONB列类型，我们只需要更改columnDefinition属性，因为json和jsonbPostgreSQL列类型都由来处理JsonBinaryType：</p>
<pre><code class="language-java">@Type(type = &quot;jsonb&quot;)
@Column(columnDefinition = &quot;jsonb&quot;)
private Location location;
 
@Type(type = &quot;jsonb&quot;)
@Column(columnDefinition = &quot;jsonb&quot;)
private Ticket ticket;
</code></pre>
<p>插入和JSONObject更新的工作方式相同，而JSONB列类型提供了更高级的查询功能：</p>
<pre><code class="language-java">List&lt;String&gt; participants = entityManager.createNativeQuery(
    &quot;SELECT jsonb_pretty(p.ticket) &quot; +
    &quot;FROM participant p &quot; +
    &quot;WHERE p.ticket -&gt;&gt; 'price' &gt; '10'&quot;)
.getResultList();
</code></pre>
<p>参考<br>
<a href="https://vladmihalcea.com/how-to-map-json-objects-using-generic-hibernate-types/">How to map JSON objects using generic Hibernate Types</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[CentOS 7 升级或新安装MariaDB 10.5，使用阿里云镜像]]></title>
        <id>https://blog.margrop.net/post/centos-7-upgrade-or-install-mariadb-10.x-aliyun-mirror/</id>
        <link href="https://blog.margrop.net/post/centos-7-upgrade-or-install-mariadb-10.x-aliyun-mirror/">
        </link>
        <updated>2021-01-29T07:42:20.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="先进行系统更新">先进行系统更新</h1>
<pre><code class="language-bash">yum update -y
</code></pre>
<h1 id="编辑yum配置文件使用阿里云镜像">编辑yum配置文件，使用阿里云镜像</h1>
<ul>
<li>这里以 MariaDB 10.5为例</li>
</ul>
<pre><code class="language-bash">vim /etc/yum.repos.d/MariaDB10.repo

#http://downloads.mariadb.org/mariadb/repositories/
[mariadb]
name = MariaDB
baseurl = https://mirrors.aliyun.com/mariadb/yum/10.5/centos7-amd64
gpgkey = https://mirrors.aliyun.com/mariadb/yum/RPM-GPG-KEY-MariaDB
gpgcheck = 1
</code></pre>
]]></summary>
        <content type="html"><![CDATA[<h1 id="先进行系统更新">先进行系统更新</h1>
<pre><code class="language-bash">yum update -y
</code></pre>
<h1 id="编辑yum配置文件使用阿里云镜像">编辑yum配置文件，使用阿里云镜像</h1>
<ul>
<li>这里以 MariaDB 10.5为例</li>
</ul>
<pre><code class="language-bash">vim /etc/yum.repos.d/MariaDB10.repo

#http://downloads.mariadb.org/mariadb/repositories/
[mariadb]
name = MariaDB
baseurl = https://mirrors.aliyun.com/mariadb/yum/10.5/centos7-amd64
gpgkey = https://mirrors.aliyun.com/mariadb/yum/RPM-GPG-KEY-MariaDB
gpgcheck = 1
</code></pre>
<!-- more -->
<h1 id="停止当前的mariadb服务">停止当前的MariaDB服务</h1>
<pre><code class="language-bash">systemctl stop mariadb
</code></pre>
<h1 id="删除旧的mariadb安装包">删除旧的MariaDB安装包</h1>
<pre><code class="language-bash">yum remove mariadb-server mariadb mariadb-libs
yum clean all
</code></pre>
<h1 id="安装-mariadb-10">安装 MariaDB 10</h1>
<pre><code class="language-bash">yum install MariaDB-server MariaDB-client
</code></pre>
<h1 id="启动并设置自动启动mariadb">启动并设置自动启动MariaDB</h1>
<pre><code class="language-bash">systemctl start mysql
systemctl enable mysql
</code></pre>
<h1 id="升级旧-mariadb-数据库">升级旧 MariaDB 数据库</h1>
<pre><code class="language-bash">mysql_upgrade -uroot -p123456
</code></pre>
<h1 id="检查是否安装成功">检查是否安装成功</h1>
<pre><code class="language-bash">mysql --version
mysql -uroot -p123456
</code></pre>
<p>参考<br>
<a href="https://www.liquidweb.com/kb/how-to-upgrade-mariadb-5-5-to-mariadb-10-0-on-centos-7/">How to Upgrade MariaDB 5.5 to MariaDB 10.0 on CentOS 7</a><br>
<a href="https://developer.aliyun.com/mirror/mariadb?spm=a2c6h.13651102.0.0.3e221b11UfYlwz">MariaDB 镜像</a></p>
]]></content>
    </entry>
</feed>